export type LibraryGame = {
  appid: number
  name: string
  /** минуты за всё время */
  playtimeForever: number
  /** минуты за последние 2 недели */
  playtime2Weeks: number
  /** unix-секунды последнего запуска; 0/undefined если Steam не отдал */
  lastPlayed?: number
}

export type GameMeta = {
  appid: number
  name: string
  /** пользовательские теги Steam: тег -> голоса */
  tags: Record<string, number>
  genres: string[]
  /** id категорий Steam (1 Multi-player, 2 Single-player, 9 Co-op, 38 Online Co-op...) */
  categories: number[]
  shortDescription?: string
  /** Обложка 460×215; оставлена для совместимости, дублирует art.header */
  headerImage?: string
  /** Резолвленные ссылки на арт всех размеров */
  art?: import('./art').GameArtUrls
  screenshots?: string[]
  isFree?: boolean
  /** цена в минимальных единицах валюты (копейки/центы) */
  priceFinal?: number
  /**
   * Цена без скидки, в тех же единицах. Вне распродажи равна priceFinal —
   * Steam в этом случае поля не присылает вовсе, и мы дублируем финальную:
   * «скидки нет» должно быть отличимо от «про цену ничего не известно».
   */
  priceInitial?: number
  /** Размер скидки в процентах. Ноль — цена полная, undefined — не спрашивали. */
  discountPercent?: number
  /** Когда скидка сгорит (unix-секунды), если Steam назвал срок */
  discountEndsAt?: number
  /**
   * Когда цена и скидка были замерены (unix-секунды).
   *
   * Отдельно от updated_at, и это не дубль: метаданные живут две недели, а
   * скидка — дни. Без своей отметки «−70%» висело бы на карточке ещё десять
   * дней после конца распродажи, и это была бы уже неправда, а не неточность.
   */
  priceAt?: number
  releaseDate?: string
  /** медиана наигранного (минуты), из SteamSpy */
  medianForever?: number
  /** год выхода — для определения серий; парсится из releaseDate в любой локали */
  releaseYear?: number
  developer?: string
  publisher?: string
  /** всего отзывов и доля положительных — сигналы актуальности */
  reviewsTotal?: number
  reviewsPercent?: number
  /** отзывов за последние 30 дней — запасной сигнал актуальности */
  reviews30d?: number
  /** игроков прямо сейчас — главный сигнал «можно ли в это играть сегодня» */
  ccu?: number
  /** когда замерен онлайн (unix-секунды) */
  ccuAt?: number
  /** магазин вне Steam (epic/battlenet/riot/...); отсутствие = Steam */
  store?: string
  /** страница игры в её магазине */
  storeUrl?: string
  /**
   * Вердикт офлайн-курации каталога. Пишется одним UPDATE в promote-catalog,
   * копируется в прод publish-catalog и до сих пор молча терялся при чтении:
   * колонки приезжали из `SELECT *` и выбрасывались в JS — та же история, что
   * была с developer и release_year.
   *
   * Три поля читаются как единое целое и появляются только вместе, потому что
   * вместе и пишутся. Ценность в первую очередь у signalsAt: непустой означает,
   * что appid прошёл через games-only пайплайн (catalog_ingest наполняется из
   * раздела «Игры» магазина), то есть это точно игра, а не саундтрек или SDK.
   * У прогретой в рантайме записи их нет вовсе — судить не по чему.
   */
  signalsAt?: number
  alive?: boolean
  supersededBy?: number
  /**
   * Почему курация сочла запись мёртвой — только при alive === false. Колонка
   * dead_reason пишется тем же UPDATE, что и три поля выше, и терялась при
   * чтении точно так же: страница мёртвой игры предлагала «Запустить» там,
   * где каталог уже знал, что матч не соберётся. См. deadVerdict.
   */
  deadReason?: import('./liveness').DeadReason
  /**
   * Что игра требует от человека: оси, длина сессии, время до веселья.
   * Живёт в своей таблице game_semantics, а не в колонке games: заливка
   * каталога (publish-catalog) и апсерт меты её не трогают. Приезжает только
   * из выборок пачкой (getGamesMeta, getGamesMetaLite, пул открытий) и со
   * строкой карточки игры (getGamePageRow); undefined — не посчитана или
   * записана в чужой версии формата.
   */
  semantics?: GameSemantics
}

/**
 * Семантика игры без модели: что игра требует от человека. Выводится
 * lib/semantics.ts из тегов Steam (приор) и уточняется разбором отзывов
 * (lib/reviewmine.ts), когда их набралось достаточно.
 *
 *   axes       — 0..100, 50 — «ничего особенного»: challenge — сложность,
 *                complexity — сколько надо освоить, pace — темп (0 — медленно);
 *   session    — сколько минут нужно на заход; canStopAnytime — можно ли
 *                бросить посреди, не потеряв прогресс (пошаговое, новелла);
 *   timeToFun  — когда игра раскрывается: fast — с первых минут, slow — после
 *                пары часов; hours — число, названное в отзывах;
 *   confidence — 0..1: по одним тегам не выше 0.4;
 *   n          — сколько отзывов на русском и английском разобрано;
 *   basis      — 'tags+reviews', только если отзывы сдвигали оси.
 *
 * v — версия формата: хранимая семантика с другой v читается как отсутствующая.
 */
export type GameSemantics = {
  v: 1
  axes: { challenge: number; complexity: number; pace: number }
  session: { bucket: 'short' | 'medium' | 'long'; minutes: number; canStopAnytime: boolean }
  timeToFun: { bucket: 'fast' | 'slow' | null; hours: number | null }
  confidence: number
  n: number
  basis: 'tags' | 'tags+reviews'
}

export type Mood = {
  time: 'short' | 'medium' | 'long'
  vibe: 'chill' | 'engaged'
  social: 'solo' | 'friends'
}

/**
 * Откуда взялся кандидат. Порядок значим: он же порядок приоритета при
 * наборе разнообразия в heuristicPicks.
 *
 * 'untouched' и 'backlog' — обе про непройденное, но это разные разговоры:
 * первую даже не скачивали, вторую открыли и закрыли. Отдельный источник, а не
 * флаг рядом с 'backlog', именно потому, что различие ведёт свой бейдж, свой
 * шаблон причины и свою строку в промпте — Record<CandidateSource, …> не даст
 * собраться, пока для нового источника не написана вся копия.
 *
 * 'familiar' — любимое без финала, в которое давно не заходил: «управление ты
 * знаешь». Стоит после 'comeback', но гарантированного слота по умолчанию не
 * получает (см. heuristicPicks): знакомое — ответ, когда нет сил на новое, а
 * не обязательная часть каждой выдачи.
 */
export const CANDIDATE_SOURCES = ['untouched', 'backlog', 'comeback', 'familiar', 'new'] as const

export type CandidateSource = (typeof CANDIDATE_SOURCES)[number]

/**
 * Реестр множителей скора — и он же порядок умножения.
 *
 * score — ровно произведение частей, и это инвариант: части нужны, чтобы
 * объяснять выдачу («лучше всех под настроение»), а объяснение, расходящееся
 * с настоящим порядком, хуже никакого.
 *
 *   taste    — косинус с профилем вкуса, взвешенный редкостью тегов, когда
 *              есть карта тегов (или популярность при пустом профиле);
 *   mood     — вайб и длина сессии;
 *   source   — наклон источника (нераспакованное вперёд);
 *   deal     — скидка, только у не купленного;
 *   lean     — ось состояния рядом с настроением (lib/mood.ts, Lean):
 *              наклон источника и штраф хардкору; без оси 1;
 *   cooldown — пауза после скипа (cooldownOf): ×1.1 у вернувшегося «не
 *              сейчас», доля месяца у «надоела», 0.5 у скрытой, которую
 *              вернул пол; без паузы 1.
 *
 * Раньше множитель жил в пяти местах сразу: в типе, в сборке частей, в
 * scoreOfParts, в тесте, который перемножал их по именам, и в бейджах. Забытое
 * умножение в scoreOfParts давало карточку, у которой части в ответе не
 * сходятся со скором, — и ни одной ошибки. Теперь тип выводится отсюда,
 * scoreOfParts сворачивает части в этом порядке, а тест сверяет ключи частей
 * каждого кандидата с реестром: новый множитель — это строка здесь и число в
 * сборке, а забытый роняет прогон.
 *
 * Новые множители — только в хвост: плавающая точка не ассоциативна, и пока
 * новый множитель единичный, скор старых кандидатов не меняется до бита
 * (демо-пятёрки главной на этом стоят).
 */
export const SCORE_FACTORS = ['taste', 'mood', 'source', 'deal', 'lean', 'cooldown'] as const

export type ScoreFactor = (typeof SCORE_FACTORS)[number]

/** Из чего сложился скор кандидата — по одному числу на множитель реестра */
export type ScoreParts = Record<ScoreFactor, number>

export type ScoredCandidate = {
  appid: number
  name: string
  source: CandidateSource
  score: number
  /**
   * Необязательно ради литералов в тестах и промежуточных списков, которые
   * собираются руками. На клиент не уходит: это изнанка скоринга.
   */
  parts?: ScoreParts
}
