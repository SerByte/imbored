/**
 * Ручные поправки к русским подписям тегов Steam.
 *
 * Словарь Steam (lib/tagsru.steam.ts) генерируется и перезаписывается целиком,
 * а этот файл генератор не трогает никогда: здесь живёт то, что Steam
 * перевёл неверно или так, что подпись спорит с соседней. Ключ — английское
 * имя тега ровно как в Steam, и его сторожит lib/tagsru.test.ts: поправка к
 * тегу, которого в словаре нет, — опечатка, и молча не сработала бы.
 *
 * Поправка — только к смыслу, не ко вкусу: «Казуальная игра» длинновато, но
 * верно, и остаётся как есть.
 */
export const TAGS_RU_OVERRIDES: Readonly<Record<string, string>> = {
  // «Приключение» в единственном числе читается как событие, а не жанр
  Adventure: 'Приключения',
  // Steam: «Яркий главный герой». Это Devil May Cry и Bayonetta — жанр боя,
  // а не характер героя
  'Character Action Game': 'Стильный экшен',
  // Steam: «Беседы» — непонятно, о чём
  'Dialogue Heavy': 'Много диалогов',
  // Steam: «Сериал» — читается как телесериал
  Episodic: 'Эпизодическая',
  // Steam: «Командная игра» — совпадает с подписью Team-Based («Командная»),
  // а party game — это игра для компании, не для команды
  'Party Game': 'Для компании',
  // Steam: «Можно приостановить» — так можно сказать про любую игру
  'Real-Time with Pause': 'Реальное время с паузой',
  // Steam: «Упрощённый рогалик» — оценка, а не жанр; так его никто не зовёт
  Roguelite: 'Рогалайт',
  // Steam: «Похожа на Dark Souls». Тот же термин, что в портрете
  // («терпеливый соулслайкер», lib/portrait.ts)
  'Souls-like': 'Соулслайк',
  // Steam: «Военные конфликты» — рядом с War («Война») и Military («Военные
  // действия») три подписи про одно и то же, а это жанр штабной стратегии
  Wargame: 'Варгейм',
}
