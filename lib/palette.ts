/**
 * Палитра ЧИСЛАМИ — для тех мест, куда не доходит globals.css.
 *
 * Обычно цвет в этом приложении берётся токеном (var(--bg) и остальные из
 * app/globals.css), и трогать литералы незачем. Но есть ровно два места, где
 * стилей нет вообще:
 *
 *   • картинки next/og — satori не исполняет CSS-переменные, и var(--bg) там
 *     превращается в пустую строку;
 *   • app/global-error.tsx — он заменяет собой корневой layout, а значит
 *     рендерится БЕЗ глобальных стилей и без шрифтов (см. docs/error.md).
 *
 * Раньше эти числа жили в lib/og.ts. Оттуда их нельзя импортировать в
 * клиентский компонент: модуль читает файлы шрифтов через node:fs прямо при
 * загрузке и утащил бы node:fs в браузерный бандл. Отсюда отдельный файл без
 * единого импорта — его безопасно тянуть откуда угодно.
 *
 * Числа обязаны совпадать с тёмной темой в globals.css: расхождение здесь
 * означает, что бренд в мессенджере или на экране аварии не тот же самый, что
 * на сайте. Совпадение сторожит lib/palette.test.ts, который читает CSS.
 */
export const BG = '#0b0c10'
export const INK = '#f2f3f5'
export const DIM = '#9ba1ab'
export const EMBER = '#ff9e64'

/** Подложка знака — то же число, что в components/Logo.tsx. */
export const PLATE = '#16171d'

/** Светлая тема: нужна там, где нет ни токенов, ни возможности их подключить. */
export const LIGHT_BG = '#f5f4f1'
export const LIGHT_INK = '#17181c'
export const LIGHT_DIM = '#5b6170'
export const LIGHT_EMBER = '#e0742f'
