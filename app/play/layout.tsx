import type { Metadata } from 'next'

/**
 * См. app/rooms/layout.tsx: страница клиентская, а metadata экспортируется
 * только из серверных модулей — отсюда отдельный layout.
 *
 * Здесь это не про выдачу: /play закрыт в robots.ts, потому что без куки он
 * всё равно выбрасывает на лендинг. Заголовок нужен человеку — вкладке,
 * истории и закладке. Главный экран продукта звался в браузере ровно так же,
 * как сам сайт, и среди нескольких открытых вкладок imbored его нельзя было
 * отличить ни от лендинга, ни от портрета.
 */
export const metadata: Metadata = {
  title: 'Подборка',
  description:
    'Игра на сегодня из твоей же библиотеки — с объяснением, почему именно она, и с числом тех, кто играет в неё прямо сейчас.',
}

export default function PlayLayout({ children }: LayoutProps<'/play'>) {
  return children
}
